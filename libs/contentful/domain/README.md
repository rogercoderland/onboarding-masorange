# contentful-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build contentful-domain` to build the library.

## Running unit tests

Run `nx test contentful-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
